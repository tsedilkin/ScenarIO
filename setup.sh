#!/bin/bash

# Цвета для вывода
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m' # No Color

echo -e "${GREEN}🚀 Начинаем установку ScenarIO...${NC}\n"

# Проверка наличия Node.js
if ! command -v node &> /dev/null; then
    echo -e "${RED}❌ Node.js не найден! Пожалуйста, установите Node.js с https://nodejs.org/${NC}"
    exit 1
fi

NODE_VERSION=$(node -v)
echo -e "${GREEN}✓ Node.js найден: ${NODE_VERSION}${NC}"

# Проверка наличия npm
if ! command -v npm &> /dev/null; then
    echo -e "${RED}❌ npm не найден! Пожалуйста, установите npm${NC}"
    exit 1
fi

NPM_VERSION=$(npm -v)
echo -e "${GREEN}✓ npm найден: ${NPM_VERSION}${NC}\n"

# Установка зависимостей корневого проекта
echo -e "${YELLOW}📦 Устанавливаем зависимости сервера...${NC}"
if npm install; then
    echo -e "${GREEN}✓ Зависимости сервера установлены${NC}\n"
else
    echo -e "${RED}❌ Ошибка при установке зависимостей сервера${NC}"
    exit 1
fi

# Установка зависимостей клиента
echo -e "${YELLOW}📦 Устанавливаем зависимости клиента...${NC}"
if cd client && npm install; then
    echo -e "${GREEN}✓ Зависимости клиента установлены${NC}\n"
    cd ..
else
    echo -e "${RED}❌ Ошибка при установке зависимостей клиента${NC}"
    exit 1
fi

echo -e "${GREEN}✅ Установка завершена успешно!${NC}\n"
echo -e "${YELLOW}💡 Для запуска проекта используйте:${NC}"
echo -e "   ${GREEN}npm run dev${NC}\n"
echo -e "${YELLOW}Или запустите скрипт start.sh для автоматического запуска${NC}\n"

