# 📺 Запуск ScenarIO на Android TV

Есть несколько способов запустить сервер на Android TV. Выберите наиболее подходящий для вас.

## ⚡ Способ 1: Termux (Рекомендуется)

Termux - это эмулятор терминала для Android, который позволяет запускать Node.js на Android TV.

### Шаг 1: Установка Termux на Android TV

**Важно:** Termux может быть недоступен в Google Play Store на Android TV. Используйте один из способов:

**Вариант A: Через F-Droid (рекомендуется)**
1. На Android TV включите "Неизвестные источники" в настройках
2. Установите F-Droid для Android TV: https://f-droid.org/
3. В F-Droid найдите и установите "Termux"

**Вариант B: Прямая установка APK**
1. На Android TV включите "Неизвестные источники"
2. Скачайте APK Termux с официального сайта: https://termux.dev/
3. Установите APK через файловый менеджер

**Вариант C: Через ADB (для продвинутых пользователей)**
```bash
adb install termux.apk
```

### Шаг 2: Установка Node.js в Termux

Откройте Termux и выполните:

```bash
# Обновление пакетов
pkg update && pkg upgrade

# Установка Node.js
pkg install nodejs

# Проверка установки
node --version
npm --version
```

### Шаг 3: Установка проекта

**Вариант A: Через Git (если есть интернет)**
```bash
# Установка Git
pkg install git

# Клонирование репозитория
cd ~
git clone https://github.com/tsedilkin/ScenarIO.git
cd ScenarIO
```

**Вариант B: Через USB/сетевое хранилище**
1. Скопируйте папку проекта на USB-накопитель или в сетевую папку
2. В Termux перейдите в папку:
```bash
cd /storage/emulated/0/Download/ScenarIO
# или
cd /sdcard/Download/ScenarIO
```

### Шаг 4: Установка зависимостей

```bash
# Установка зависимостей сервера
npm install

# Установка зависимостей клиента
cd client
npm install
cd ..
```

### Шаг 5: Настройка Ollama (опционально)

Ollama для Android TV - это сложнее. Есть несколько вариантов:

**Вариант A: Использовать Ollama на другом устройстве**
- Запустите Ollama на компьютере/телефоне в той же сети
- В Termux установите переменную окружения:
```bash
export OLLAMA_URL=http://IP_АДРЕС_КОМПЬЮТЕРА:11434/api/generate
```

**Вариант B: Без Ollama (только базовые функции)**
- Игра будет работать, но генерация через ИИ будет недоступна
- Можно использовать только ручной ввод и GIF

### Шаг 6: Запуск сервера

```bash
# Запуск только сервера (без dev-режима клиента)
npm run server
```

Или используйте скрипт:
```bash
chmod +x setup-android.sh
./setup-android.sh
```

### Шаг 7: Доступ к игре

1. Узнайте IP-адрес Android TV:
```bash
ifconfig | grep inet
```

2. Откройте браузер на любом устройстве в той же сети
3. Введите адрес: `http://IP_АДРЕС_TV:3000`

---

## 🚀 Способ 2: Нативное Android приложение (Продвинутый)

Можно создать нативное Android приложение, которое включает в себя:
- Встроенный Node.js сервер
- Веб-вью для отображения интерфейса
- Автоматический запуск при старте

**Требуется:**
- Android Studio
- Знания Java/Kotlin или React Native

**Преимущества:**
- Работает как обычное приложение
- Автоматический запуск
- Не требует Termux

**Недостатки:**
- Сложнее в разработке
- Требует компиляции APK

---

## 📱 Способ 3: Запуск на телефоне/планшете Android

Если Android TV не поддерживает Termux, можно запустить сервер на Android телефоне/планшете:

1. Установите Termux на телефон
2. Следуйте инструкциям из Способа 1
3. Подключите телефон и TV к одной Wi-Fi сети
4. Откройте игру на TV через браузер, указав IP телефона

---

## ⚙️ Настройка для работы в фоне (Termux)

Чтобы сервер работал даже когда Termux закрыт:

```bash
# Установка Termux:Boot (для автозапуска)
pkg install termux-boot

# Создание скрипта автозапуска
mkdir -p ~/.termux/boot
echo '#!/data/data/com.termux/files/usr/bin/sh
cd ~/ScenarIO
npm run server' > ~/.termux/boot/start-scenario.sh
chmod +x ~/.termux/boot/start-scenario.sh
```

---

## 🔧 Устранение проблем

**Проблема: Termux не устанавливается**
- Убедитесь, что включены "Неизвестные источники"
- Попробуйте установить через F-Droid
- Проверьте совместимость с вашей версией Android TV

**Проблема: Node.js не устанавливается**
- Обновите пакеты: `pkg update && pkg upgrade`
- Попробуйте установить через nvm: `pkg install curl && curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash`

**Проблема: Не могу найти папку проекта**
- Используйте `ls` для просмотра содержимого
- Проверьте права доступа: `termux-setup-storage`

**Проблема: Порт занят**
- Измените порт: `export PORT=3001 && npm run server`

**Проблема: Игроки не могут подключиться**
- Убедитесь, что все устройства в одной Wi-Fi сети
- Проверьте файрвол Android TV
- Попробуйте использовать IP-адрес вместо localhost

---

## 📝 Быстрый старт (Termux)

```bash
# 1. Установка Node.js
pkg update && pkg upgrade
pkg install nodejs git

# 2. Клонирование проекта
cd ~
git clone https://github.com/tsedilkin/ScenarIO.git
cd ScenarIO

# 3. Установка зависимостей
npm install
cd client && npm install && cd ..

# 4. Запуск сервера
npm run server
```

---

**Примечание:** Android TV имеет ограничения по сравнению с обычным Android. Если что-то не работает, рассмотрите вариант запуска сервера на компьютере или телефоне, а TV использовать только для отображения интерфейса через браузер.

