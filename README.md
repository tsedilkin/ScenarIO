# ScenarIO - Игра челленджей для 2 игроков

Простая инструкция по запуску игры для людей, которые не очень хорошо разбираются в компьютерах.

## 📋 Что нужно перед запуском

1. **Установленный Node.js** - это программа, которая нужна для работы игры
   - Если у вас его нет, скачайте с сайта: https://nodejs.org/
   - Выберите версию "LTS" (длинная зеленая кнопка)
   - Установите, следуя инструкциям на экране

2. **Установленный Ollama** - это программа для работы ИИ-генерации вариантов
   - Если у вас его нет, скачайте с сайта: https://ollama.ai
   - Установите, следуя инструкциям на экране
   - После установки откройте терминал и выполните команду для скачивания модели:
   ```bash
   ollama pull mistral
   ```
   - **Рекомендуется использовать модель `mistral`** вместо `llama3.2`, так как она лучше работает с эротическим контентом
   - Это займет несколько минут (модель весит около 4 ГБ)
   - После завершения вы увидите сообщение "success"
   - Если хотите использовать другую модель, установите переменную окружения: `export OLLAMA_MODEL=название_модели`

## 🚀 Как запустить игру (пошаговая инструкция)

### Важно: Все команды выполняются из корневой папки проекта (ScenarIO)

### Шаг 1: Откройте папку с проектом

1. Найдите папку `ScenarIO` на вашем компьютере
2. Откройте её (двойной клик)
3. **ВАЖНО:** Все дальнейшие команды нужно выполнять из этой папки!

### Шаг 2: Откройте терминал (командную строку)

**На Mac:**
- Нажмите одновременно клавиши `Cmd + Пробел`
- В появившемся окне введите: `Терминал` или `Terminal`
- Нажмите Enter
- В терминале введите: `cd ` (с пробелом в конце)
- Перетащите папку `ScenarIO` в окно терминала
- Нажмите Enter

**На Windows:**
- Нажмите клавишу `Win + R`
- В появившемся окне введите: `cmd`
- Нажмите Enter
- В командной строке введите: `cd ` (с пробелом в конце)
- Перетащите папку `ScenarIO` в окно командной строки
- Нажмите Enter

### Шаг 3: Скачайте модель для Ollama (если еще не сделали)

Если вы только что установили Ollama, нужно скачать модель для работы ИИ:

В терминале введите команду и нажмите Enter:

```bash
ollama pull mistral
```

**Важно:** Рекомендуется использовать модель `mistral`, так как она лучше работает с эротическим контентом для игры.

Подождите, пока модель скачается (может занять 5-10 минут в зависимости от скорости интернета). Вы увидите прогресс загрузки.

**Примечание:** Это нужно сделать только один раз. После скачивания модель будет доступна всегда.

**Альтернатива:** Если хотите использовать другую модель (например, `llama3.2`), скачайте её командой `ollama pull llama3.2`, но учтите, что она может быть более строгой к эротическому контенту.

### Шаг 4: Установите зависимости

В терминале (или командной строке) введите следующую команду и нажмите Enter:

```bash
npm install
```

Подождите, пока установка завершится (может занять 1-2 минуты). Вы увидите много текста - это нормально.

### Шаг 5: Установите зависимости для клиента

Введите следующую команду и нажмите Enter:

```bash
cd client
npm install
cd ..
```

**Или проще:** Можно выполнить все установки одной командой из корневой папки:

```bash
npm install && cd client && npm install && cd ..
```

Подождите, пока установка завершится.

### Шаг 6: Запустите игру

Введите команду и нажмите Enter:

```bash
npm run dev
```

Вы увидите сообщения в терминале. Найдите строку, которая начинается с `Network:` - это адрес вашего сервера. Например:
```
Network: http://192.168.1.114:5173
```

**ВАЖНО:** Не закрывайте это окно терминала! Игра работает, пока оно открыто.

## 🎮 Как играть

### Для Рассказчика (ведущего):

1. Откройте браузер (Chrome, Safari, Firefox - любой)
2. В адресной строке введите адрес из терминала (например: `http://192.168.1.114:5173`)
3. Нажмите Enter
4. Нажмите кнопку **"Создать сессию как Рассказчик"**
5. Запомните или запишите **ID сессии**, который появится на экране
6. Покажите этот ID игрокам

### Для Игроков:

1. Убедитесь, что вы подключены к той же Wi-Fi сети, что и компьютер с запущенной игрой
2. Откройте браузер на телефоне
3. В адресной строке введите тот же адрес (например: `http://192.168.1.114:5173`)
4. Введите **ID сессии** от Рассказчика
5. Нажмите **"Присоединиться как Игрок"**
6. Заполните карточки с действиями
7. Нажмите **"Готов"**, когда закончите

### Что происходит дальше:

- Когда оба игрока нажмут "Готов", карточки автоматически перевернутся
- Рассказчик увидит результаты на большом экране
- Рандомайзер выберет победителя для каждой пары карточек
- Победившие карточки будут подсвечены зеленым

## ⚠️ Важные моменты

1. **Все устройства должны быть в одной Wi-Fi сети** - и компьютер, и телефоны
2. **Не закрывайте окно терминала** - игра работает только пока оно открыто
3. **Если что-то не работает:**
   - Проверьте, что все устройства в одной Wi-Fi сети
   - Убедитесь, что окно терминала открыто
   - Попробуйте перезапустить игру (закройте терминал, откройте снова и выполните `npm run dev`)

## 🛑 Как остановить игру

1. В окне терминала нажмите `Ctrl + C` (на Mac может быть `Cmd + C`)
2. Подождите несколько секунд
3. Закройте окно терминала

## 📦 Сборка для production (опционально)

Если вы хотите собрать игру для постоянного использования (без режима разработки):

1. **Из корневой папки проекта** (ScenarIO) выполните:
```bash
npm run build
```

Эта команда соберет клиентскую часть и подготовит её для production.

2. После сборки запустите сервер:
```bash
npm start
```

3. Игра будет доступна на `http://[ваш-ip-адрес]:3000`

**Примечание:** Для обычной игры используйте `npm run dev` - это проще и удобнее для разработки.

## 📱 Доступ с других устройств

Чтобы игроки могли подключиться с телефонов:

1. Узнайте IP-адрес вашего компьютера:
   - **На Mac:** Откройте "Системные настройки" → "Сеть" → найдите IP-адрес
   - **На Windows:** Откройте командную строку, введите `ipconfig`, найдите "IPv4-адрес"
2. Используйте этот адрес вместо `localhost` или `192.168.1.114`
3. Например, если ваш IP `192.168.1.100`, то адрес будет: `http://192.168.1.100:5173`

## ❓ Частые проблемы

**Проблема:** Не могу установить зависимости
- **Решение:** Убедитесь, что Node.js установлен. Перезапустите терминал и попробуйте снова.

**Проблема:** Игроки не могут подключиться
- **Решение:** Проверьте, что все устройства в одной Wi-Fi сети. Проверьте IP-адрес в терминале.

**Проблема:** Игра не запускается
- **Решение:** Убедитесь, что вы находитесь в правильной папке (ScenarIO). Проверьте, что все зависимости установлены.

**Проблема:** Карточки не отображаются
- **Решение:** Обновите страницу в браузере (F5 или Cmd+R). Убедитесь, что оба игрока нажали "Готов".

---

**Приятной игры! 🎉**
